import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

export const LoadingWheelBox = styled(Box)(({ theme }) => ({
    backgroundColor: '#B3F7DC',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
}));