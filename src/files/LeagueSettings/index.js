import React from 'react';
import {
    Root,
    Title,
} from './LeagueSettings.styles';

export default function LeagueSettings() {

    return (
        <Root>
            <Title variant="h5">settings later</Title>
        </Root>
    );
}